import { Factory } from '@design/creational/factory';

describe('Factory design pattern', () => {
    describe('Factory decorator', () => {
        it('should add metadata to factory class', () => {});
    });
});
